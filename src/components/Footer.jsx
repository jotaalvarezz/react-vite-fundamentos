const Footer = () => {
    return (
        <footer>
            <p className="mt-14 text-center dark:text-gray-300">
                Drag and Drop to reorder list
            </p>
        </footer>
    );
};

export default Footer;
